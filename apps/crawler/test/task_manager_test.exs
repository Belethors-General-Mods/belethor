defmodule TaskManagerTest do
  use ExUnit.Case
  require Logger


  @tag capture_log: true
  test "basic test ensure provider.search(query) gets called"

  @tag capture_log: true
  test "don't hang on unending tasks"

  @tag capture_log: true
  test "allow multiple managers running at the same time"

end
